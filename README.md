

将输入框分为 3 个
- prefix：前缀文字 length：40
- middle：中间文字 length：100
- suffix：后缀文字 length：20

* 长度配置均从配置文件或环境变量文件中读取 以上是如果没有读取则使用的默认值

以及顶部展示一个只读的 最终 发送的信息


在频道的底部添加一个新的功能用于快捷输入 prefix(活动) content(职业) suffix(备注)

prefix：内容主要为开团的副本信息
    - NAXX双龙百元均分
content: 主要为需要招募的职业，以来默认语句开头，全部为选项 打勾选择 选择后拼接 ( eg: 来 JLM/NQ/FQ )
    - JLM
    - FQ
    - DKT
suffix: 主要内容为 某些职业位置已满了
    - T满
    - N满
    - TN满
    - 近战满
    - 远程满

* 以上配置 都使用 json 配置 方便后期修改


首先功能没问题 ，接着修改交互 底部的添加 可能会有很多 所以 底部应该为 顶部是 tab 切换 然后在  

1. 活动列表应该是多个如果多了 则可以滚动查看


首先功能没有问题
1. 调整样式 底部的 快捷模版块 和输入框椅杨有一个背景
2. 除了活动这样滚动单行展示
3. 职业和备注都是 从上到下 从左到右的 按钮 的checkbox 展示


帮我给 职业 选择 进行分类展示，也可以改变配置文件 通过配置文件的分类展示 
- T
- N
- 近战DPS
- 远程DPS
等
